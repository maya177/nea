work on database attribute diagram

scale values and get recent thresholds then compare in /compa

if page only accessible thro home page which is onyl accessible if logged in, is session handling required?